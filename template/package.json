{
  "name": "my-node-app",
  "version": "1.0.0",
  "description": "",
  "author": "Michal Korman",
  "license": "MIT",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "lint": "eslint --ext .ts src/",
    "fix": "eslint --ext .ts --fix src/",
    "test": "jest --detectOpenHandles --forceExit",
    "build": "tsc --build",
    "start": "tsc-watch --noClear --onSuccess \"node .\"",
    "exec": "exec node --enable-source-maps ."
  },
  "devDependencies": {
  },
  "dependencies": {
  }
}
