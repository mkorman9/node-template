{
  "name": "node-template-project",
  "version": "1.0.0",
  "description": "",
  "author": "Michal Korman",
  "license": "MIT",
  "private": true,
  "main": "lib/index.js",
  "scripts": {
    "lint": "eslint --ext .ts src/",
    "fix": "eslint --ext .ts --fix src/",
    "test": "jest --detectOpenHandles --forceExit",
    "build": "tsc --build",
    "dev": "tsc-watch --noClear --onSuccess \"node --enable-source-maps .\"",
    "start": "exec node --enable-source-maps ."
  },
  "devDependencies": {
  },
  "dependencies": {
  }
}
